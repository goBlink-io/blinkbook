import { A as AdapterHookResult } from '../types-Dgo4Mqar.js';

/**
 * Sui adapter — uses @mysten/dapp-kit.
 * Note: Sui connect is handled via the SuiConnectButton rendered in the modal.
 */
declare function useSuiAdapter(): AdapterHookResult;

export { useSuiAdapter };
